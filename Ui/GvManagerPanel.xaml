<UserControl x:Class="Lumen.Ui.GvManagerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <UserControl.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#2D2D30"/>
                        <Setter Property="Foreground" Value="#F0F0F0"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#094771"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="Toggle" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                            Foreground="{TemplateBinding Foreground}"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            Focusable="False" Padding="4,3">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Background" Value="#2D2D30"/>
                                        <Setter Property="BorderBrush" Value="#3F3F46"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2"
                                                            SnapsToDevicePixels="True">
                                                        <Grid TextElement.Foreground="#F0F0F0">
                                                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0"
                                                                                TextElement.Foreground="#F0F0F0"/>
                                                            <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#AAAAAA"
                                                                  HorizontalAlignment="Right" Margin="0,0,6,0"
                                                                  Width="8" Height="4" Stretch="Uniform"
                                                                  VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Arrow" Property="RenderTransform">
                                                                <Setter.Value>
                                                                    <ScaleTransform ScaleY="-1" CenterX="4" CenterY="2"/>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <Popup x:Name="Popup" IsOpen="{Binding IsChecked, ElementName=Toggle}"
                                   AllowsTransparency="True" Placement="Bottom" PlacementTarget="{Binding ElementName=Toggle}"
                                   StaysOpen="False" MaxHeight="300">
                                <Border Background="#252526" BorderBrush="#3F3F46" BorderThickness="1" CornerRadius="2"
                                        SnapsToDevicePixels="True" Padding="4">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Popup" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="#1E1E1E" BorderBrush="#3F3F46" BorderThickness="1"
            CornerRadius="6" Padding="0" Width="420" MaxHeight="540">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏（X 关闭） -->
            <Border Grid.Row="0" Background="#2D2D30" CornerRadius="6,6,0,0"
                    Padding="10,6" MouseLeftButtonDown="Header_MouseDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="全局变量 (gv)" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF"/>
                    <Button Grid.Column="1" Content="✕" Width="24" Height="22" FontSize="12"
                            Padding="0" Click="CloseBtn_Click" ToolTip="关闭"/>
                </Grid>
            </Border>

            <TextBlock Grid.Row="1" Text="跨原子/跨页共享，可在属性编辑器的「变量」态引用，或在公式里用 gv(name) 读取。"
                       FontSize="10" Foreground="#8A8A8A" TextWrapping="Wrap" Margin="14,8,14,8"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="380" Margin="14,0,14,0">
                <StackPanel x:Name="List" Orientation="Vertical"/>
            </ScrollViewer>

            <Border Grid.Row="3" Background="#252526" CornerRadius="0,0,6,6" Padding="14,8">
                <Button x:Name="AddBtn" Content="＋ 新增变量" HorizontalAlignment="Left" Padding="10,4"
                        Click="AddBtn_Click"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
