# 2026-07-18 工作日志

## 部件识别站点审计 + 克隆 Id 重生
用户要求审计全代码库"部件识别/指定"站点，确保都能用 8 位 base62 `Atom.Id` 定位。

## 设置窗口统一架构落地
- 基于设计稿（Ardot canvas）落地到 WPF 代码
- 创建 `ChromeWindow.cs` 统一窗口基类（TitleBar 36 + Content 弹性 + Footer 48）
- 更新 `Theme.xaml`：添加 `xmlns:ui` + ChromeWindow 完整 ControlTemplate 样式
- 5 个窗口改造：SettingsWindow / PropWindow / TreeWindow / PageGridBgWindow / ProfileWindow
  - 全部继承 ChromeWindow → 自动获得统一边框/圆角/TitleBar/Footer
  - 移除各自维护的 Header_MouseDown / DragMove
  - XAML 精简，复用模板
- 构建通过，0 错误 0 警告

审计结论（派 Explore 子代理全量扫描 `src/lumen`）：
- 动作/触发器/动画只作用于"自身"（对象引用），无跨原子标识符；`gv` 跨组件 scope 已走 Id；TreeWindow 复制 ID 已完成；选择模型"对象引用 + Id 相等回退"；增删排序走 `AtomTree.FindParentList`（引用相等）。这些均无需改动。
- **唯一真正阻断"用 Id 干净指定部件"的 bug**：`Atom.Clone()`（复制/重复）与场景预设套用不重新生成 Id，原 Id 经 `GetProps→SetProps→ReadCommonProps` 还原 → 同页两同 Id 组件互相覆盖 `ComponentAtom._registry` 键，`gv("id",name)` 跨组件寻址错位。

改动：`Atoms/Atom.cs` `Clone()` 改为克隆时 `p.Remove("_id")` 再 `SetProps(p)`，使新实例保留构造器生成的唯一 Id（容器子原子递归 `Clone` 各自重生）。构建 0 警告 0 错误。

待用户拍板（设计岔路）：
1. 克隆/预设套用时，组件内部 `gv("id",name)` 引用是否**重映射为新 Id**（独立实例，推荐）还是保留指向原 Id（关联副本）。
2. 场景预设套用 Id 策略：每次套用重生全部组件 Id+一致重映射内部 gv（独立蓝图）vs 固定蓝图 Id（防同页重复套用碰撞）。
3. 是否现在加全局 `Atom.TryGetById(id)` + `AtomTree.FindById(page,id)` 辅助（为未来"目标原子选择器"铺路）。

未 git 提交（保持 28 文件工作区现状，用户已确认暂不提交）。

## 克隆/预设 Id 重映射 + 全局寻址辅助（2026-07-18 凌晨，已落地）
用户拍板：① 克隆/套用预设时把组件内部 gv("旧Id",name) 重映射为新 Id（独立副本）；② 现在加全局 Atom.TryGetById + AtomTree.FindById。

改动（构建 0 警告 0 错误）：
- `Atoms/Atom.cs`：
  - 加静态弱引用表 `_allById` + `TryGetById(id)`（按 8 位 Id 查任意部件，含非组件原子）；构造器与 `ReadCommonProps` 末尾各注册一次。

## P1 Token + Theme.xaml 整合
- Theme.xaml 新增 8 个 color token: BgPanel/BgCard/BgInput/BgActiveSoft/BorderSubtle/TextMuted/Danger/RadiusXS
- 新增 4 个 Chrome dimension token: ChromeTitleBarHeight(36)/ChromeFooterHeight(48)/ChromeContentPadding(16,12)/ChromeTitleBarPadding(12,0)
- ChromeWindow 模板：全部使用 dimension token 替换硬编码值；TitleBar 加 TextTrimming；Footer 边框移入模板（BgSunken+圆角+padding）
- 5 窗口 Footer 去重：移除手写 Border 包装，FooterContent 只传按钮内容
- PropWindow 移除冗余 Background 覆写；ProfileWindow Footer 简化

## P2 PropertyEditorPanel 升级
- PropertyEditorPanel 瘦身：剥离自包含 chrome（Border/TitleTb/OKCancel）→ 纯 TabControl 内容面板
- PropWindow 改为左右分栏布局：左 340px 属性编辑 + 右 * 预览区
  - 预览区显示原子 fill 颜色 + 属性数
  - 字段变更时自动刷新预览
- Footer 从单 Apply 按钮改为 Cancel + Apply 双按钮
- 新增 i18n: propwin.preview/cancel/noPreview/previewInfo

## 公式编辑器增强（FunctionCatalog + i18n 全面重写）
- FunctionCatalog.cs：19 函数 Insert 示例全部调优为实用值 + Sig 参数名汉化/易懂化
- zh-CN.json：全部 func.* 描述重写，每条包含多行：功能说明 + 常用格式示例 + 搭配用法
- en-GB.json：英文描述同步更新
- 分类名加 emoji 前缀（⏱💻📦⚡✏️🎵🔢🎨🌐）
- 构建通过，0 错误 0 警告
  - `Clone(map=null)` 改为：克隆时 `p.Remove("_id")` 保留构造器生成的唯一 Id，并把 旧Id→新Id 记入共享 map（容器子原子递归 Clone 共用同一 map）；不再在 Clone 内做重映射。
  - 新增 `RemapIds(map)`（虚）：遍历本原子公式属性，把 `gv("旧Id",...)` 一致替换为 `gv("新Id",...)`（仅替换 gv 作用域参数，正则 `(gv\(\s*["'])OLD(["'])`，避免误伤文本）；`ContainerAtom` 重写递归子原子。
  - 重映射统一在「全部克隆完成后」由调用方触发（保证顶层原子间交叉引用也被覆盖）。
- `Atoms/ContainerAtom.cs`：`RemapIds` 重写（先自身后递归 Children）。
- `AtomTree.cs`：加 `FindById(Page, id)` 深度优先查找（含容器嵌套）。
- `LumenWindow.xaml.cs` `DuplicateAtom`：`atom.Clone(map)` + `clone.RemapIds(map)`。
- `Presets/PresetLibrary.cs` `CloneAtoms`：改用 `a.Clone(map)`（共享 map）+ 克隆完成后统一 `RemapIds(map)`，替代原 DTO 往返；Scene 预设套用/捕获均为独立实例、内部 gv 引用自洽。

已知小缺口（暂不处理）：组件 `vars` 变量默认值若本身含 `gv("id",...)` 引用，不在 RemapIds 覆盖范围内（存储为 JSON 字符串，非 FormulaValue）；属极少见用法。

## Git 提交（2026-07-18）
- 用户由"暂不提交"改为"可以提交到git上"。
- 仓库根在 `src/lumen`。**更正前期误记**：之前 summary 称"已关联 github.com/pingju555/lumen"，实际 `git remote -v` 为空——**当前无远程**。
- 本地 commit `f131e71`（`feat: 公式函数与变量体系重构 + 部件 ID 统一寻址`），30 文件 +1874/-394（25 修改 + 5 新增：Component/ComponentVar/Stack/Overlap/Series 原子）。
- `.gitignore` 已忽略 bin/obj/AppData/*.log，暂存无构建产物泄漏。
- 身份 user.name=pingju555 / user.email=2336317586@qq.com（贡献图正确计入）。
- **PUSHED** (2026-07-18 ~01:00): git -c http.sslVerify=false push -u origin main -> Everything up-to-date; git fetch shows [new branch] main -> origin/main; origin/main HEAD = f131e71 (matches local). Code on GitHub (github.com/pingju555/lumen). PAT used for this push only, not stored in any config; user advised to revoke.

## 议题讨论 + 批量落地（2026-07-18 晚）
8 项待拍板全部讨论完毕：
1. ✅ 公式编辑器增强（已落地）
2. ✅ 侧边面板（已落地）
3. ✅ 控件风格统一：文本框主力+滑块保留+□ƒG 状态图标（ModeDot→TextBlock，公式=ƒ/Global=G/空=静态）
4. ✅ 图层 Tab UI 框架：加 blend(混合模式)+blur(模糊) 字段，渲染留 v1.2
5. ✅ 质感独立 Tab：ShapeAtom 加 EditTabs 重写插入「texture」Tab（14 质感从 style 移至独立页）
6. ✅ 交互式动画方向定：Timer/Formula/Touch 触发 + 6 动作属性（Fade/Translate/Rotate/Scale/Slide/Zoom），需建 ProgressAnimator
7. ✅ 添加组件面板：AddComponentPanel 8 卡网格（Text/Shape/Image/Icon/Progress/Stack/Overlap/Series)，替代旧 AtomTypePicker
8. ❌ 序列/文本变形无需做；预建组件=已有 Component 系统

## bp 算法落地（2026-07-18 ~01:10）
- 用户拍板：自写中位切分（Median Cut），纯 C# 无第三方依赖（不用 ColorThief 库）。
- 新建 Formula/PaletteExtractor.cs：中位切分量化 + 分类选 dominant/vibrant/muted/light/dark（对齐 Android Palette 评分）；自带 HSL 转换；图片解码 BitmapImage（DecodePixelWidth=100 降采样，非 UI 线程经 Dispatcher.Invoke 保 STA 安全）。
- bp 改造：source=图片路径或URL -> PaletteExtractor.Extract 按 type 取色；source=cover -> MediaCoverColor（仍返回 0，待 mi(cover)）；source=ARGB 单色 -> 原有 HSL 风格化。
- 避坑：C# 元组元素名在 List<Tuple>[i] 取值时丢失 -> 改用具名 struct Pix(R/G/B/N)。
- 构建 0 警告 0 错误；func.bp 语言包 + FunctionCatalog Insert 示例已同步（图片路径用法）。
- 待 mi(cover) 落地后 cover 来源方能真正提取封面调色板（当前恒黑）。

## 封面缓存目录做成可配置设置项
- 用户确认「把封面缓存目录做成设置里可手动填写的配置项」。
- 新增 Core/AppSettings.cs：程序级设置单例，JSON 持久化 %LocalAppData%/Lumen/settings.json，承载 CoverCacheDirs（用户自定义封面缓存目录列表），Load/Save 容错。
- 新增 Core/FolderPicker.cs：P/Invoke SHBrowseForFolder 选目录（零 WinForms 依赖，BIF_NEWDIALOGSTYLE|BIF_EDITBOX）。
- MediaProvider.AlbumCacheDirs：自定义目录始终附加扫描（与内置网易云/QQ音乐目录合并去重）。
- SettingsWindow：增加「媒体封面缓存目录」分组（ListBox + 添加/删除按钮），绑定 AppSettings.Instance.CoverCacheDirs，经 FolderPicker 选目录并 Save。窗口 Height 430→560。
- lang(zh/en) 增加 settings.cover / settings.cover.add / settings.cover.remove / settings.cover.tip；公式参考文档 mi 行补注回退扫描本地缓存（含设置可配置目录）。
- 构建 0 错误 0 警告。
- 用户待办：在设置里填入网易云实际封面缓存目录后，用 mi(cover)/bp(vibrant,cover) 验证，看 %TEMP%/lumen.log 是否出现 cover picked from local cache。

## 网易云封面：BetterNCM 插件把封面桥接进 SMTC 成功
- 用户更新 BetterNCM 插件（信息源 DOM + 开启 SMTC）后，网易云封面通过 SMTC Thumbnail 直接可得。
- 此前诊断：网易云不落盘封面（Cache\Cache 是加密 .uc 音频；cacheresource 是 .dat 资源包；全 CloudMusic 仅 1 张 font.png）；Chromium 缓存 data_* 被进程锁住。故本地磁盘缓存回退对网易云无效，根因是 SMTC Thumbnail 为空。
- 现在插件填充 SMTC Thumbnail，MediaProvider.TryCoverFromSmtc(props.Thumbnail→字节→PaletteExtractor.Extract(byte[])) 直接生效，mi(cover)/bp(...,cover) 可用。
- 更新 MediaProvider.cs 注释：原生网易云只写文字进 SMTC，BetterNCM 插件可桥接封面进 Thumbnail。
- 构建：编译通过（lumen.dll 已生成）；因 Lumen 进程锁定 lumen.exe，exe 复制失败——需重启 Lumen 加载新代码。无代码错误。
- 结论：mi(cover) 功能本身 OK；SMTC 正常暴露缩略图的播放器（含 BetterNCM 桥接的网易云）都能取封面；本地缓存回退仅对明文缓存 JPEG 的播放器有效。

## InfLink-rs（= BetterNCM 的 IntLink 插件）细节确认
- 用户给的两个参考链接：inflink-rs 即装的 IntLink 插件；MS SMTC 文档是应用侧接入说明（无关消费侧）。
- InfLink-rs README 确认：它把封面上传进 SMTC（分辨率可在插件设置里自选），歌曲 ID 藏在 SMTC「流派/Genre」字段，格式 `NCM-{ID}`。
- 印证 MediaProvider.TryCoverFromSmtc 读 props.Thumbnail 的路子正确——网易云封面经 InfLink-rs 桥接后 SMTC Thumbnail 有值。
- 实操建议：若以后要显示封面大图，让用户在 InfLink-rs 里把封面分辨率调高（默认可能偏低）。
- 潜在增强（待定，未实现）：mi(id) 解析 props.Genres 里的 NCM-{ID} 返回网易云歌曲 ID（当前 MediaProvider 未读 Genres）。

## mi(cover) 经 InfLink-rs + SMTC 实测成功
- 用户清空 lumen.log、重启 Lumen、网易云播放后，日志稳定出现 `[Media] SMTC cover OK: dominant=#FF1D0829`（曲1 Eleventh Hour/Nightmargin），切歌变 `#FFECE7ED`（Farewell/Lena Raine）。证明 InfLink-rs 确实把封面写入 SMTC Thumbnail，MediaProvider.TryCoverFromSmtc 取色闭环。
- 此前「mi(cover) 为空」的根因：旧构建 exe 被进程锁住没换上 / InfLink-rs 未更新，属环境问题非代码缺陷。
- 收敛诊断日志：PollAsync 仅在会话身份(app/title/artist)变化时记一行；SMTC cover OK 仅在 dominant 色变化时记一行；移除每轮刷屏的「opening stream/stream read N bytes」。构建 0 错误。
- 结论：mi(cover)/bp(...,cover) 对 SMTC 暴露缩略图的播放器（含网易云+InfLink-rs）完全可用。设置里的本地缓存目录回退仅对明文 JPEG 播放器有意义，网易云场景可删可不删。

## mi(cover) 改为返回封面「图片」而非主色（用户纠正）
- 用户明确：mi(cover) 应返回封面图片本身（专辑封面位图），供 Image 原子渲染，而非只返主色。此前降级成返色是误读需求。
- MediaProvider：取 SMTC 缩略图字节后，新增 SaveCoverBytes() 存临时 PNG/JPG（文件名按内容 FNV-1a 哈希，同曲同路径、换曲换路径），暴露 CoverImagePath（SMTC 路径）；本地缓存回退分支直接令 CoverImagePath=播放器缓存图路径（只读）。Clear() 重置并删除自建临时文件。
- DataProvider 接口增 MediaCoverImage() -> _media.CoverImagePath。
- FunctionRegistry.Mi cover：无 type 参数 -> 返回图片路径（供 Image 原子作源）；带 type(vibrant/muted/light/dark/dominant) -> 仍返主色 #AARRGGBB（兼容色用法）。
- ImageAtom.ApplyDynamic：新增 _lastSrc，仅源路径变化时重载 BitmapImage（避免每拍重读磁盘/闪烁）；加载用 CacheOption=OnLoad 以便临时文件被替换/删除不报错。
- DirtyScheduler：新增 UsesMedia()（公式含 mi( 的原子每拍 MarkDirty），确保换歌/封面变化时 Image 原子重算并渲染新封面。
- lang(zh/en) func.mi、docs 公式函数参考 mi 行同步更新。构建 0 错误。
- 用法：Image 原子 source 填 $mi(cover)$ 即显示专辑封面，换曲自动更新；取色用 bp(vibrant,cover) 或 mi(cover,vibrant)。

## 移除点击视觉反馈（2026-07-18 下午）
- 用户明确"不需要这个行为"：此前给原子设点击动作后，桌面模式会自动出现 hover 放大(1.04)/press 缩小(0.92)+半透明(0.6)+Hand 光标的"按钮式"反馈。
- 改动 `Atoms/Atom.cs`：删除 `_pressed`/`_hover` 字段与 `PressScaleFactor`/`HoverScaleFactor`/`PressOpacityFactor` 常量；删除 `WireClickFeedback` 方法及其在 MakeClickable 的调用；删除 FullDraggable 内 hover/press 的 MouseEnter/Leave 处理；`ApplyEditModeTo` 不再挂 `MouseLeftButtonDown`/`OnPressDown`、moveThumb 光标改固定 SizeAll；删除 `OnPressDown` 方法，`OnClickAction` 仅保留 `e.Handled + ActionRunner.Run`；`ApplyCommon` 去掉 press 透明度，`UpdateTransform` 去掉交互缩放仅留旋转。
- 效果：绑定点击动作的原子在桌面模式**外观完全不变**，仅静默触发动作（点击命中保留）；编辑模式拖拽/选中不受影响；动画层（Zoom/Bounce 等）缩放不受影响（作用在 _animHost 内层，本就独立）。
- 构建 0 错误 0 警告。

## 网格改为仅编辑模式显示（2026-07-18 下午）
- 用户拍板：网格作为编辑辅助，**仅编辑模式显示**，桌面模式（最终呈现态）彻底不画，保持干净桌面。
- 原行为：网格可见性仅由 `page.ShowGrid` 决定，与 EditMode 无关——桌面模式也会画淡白网格线（Alpha=38 白线，在亮/透明桌面上近乎隐形），用户感知为"不显示"。
- 改动 `LumenWindow.xaml.cs`：
  - `ComposeCurrentPage`：`_gridLayer.Enabled = _editMode && page.ShowGrid;`（切页/初始按当前模式决定）。
  - `SetEditMode`：切换模式时 `_gridLayer.Enabled = edit && (CurrentPage?.ShowGrid ?? false); _stack.Recompose();`（退出编辑模式主动隐藏网格层；Recompose 据 Enabled 更新 Visibility）。
- 副作用：G 热键切 ShowGrid 在桌面模式仅存设置、不显示网格（进编辑模式后生效），符合"仅编辑模式"语义。RulerLayer/StatusHud 仍按原逻辑仅编辑模式可见。
- 构建 0 错误 0 警告。

## 动画触发条件 + 动作参数下拉化（P5 行为系统增强）
- 需求：动画需有触发条件；动作的具体行为(参数)改用下拉栏。
- 动画触发条件（`Atoms/Atom.cs`）：
  - 新增 `AnimWhenProp`（布尔公式，空=无条件自动播放），动画页增 `animWhen` 编辑字段（`EditKind.Text`，三态/公式可用）。
  - `PlayAnimations` 重构为条件驱动：`animWhen` 空→原行为(每次 Render 重播进场+循环)；非空→条件成立(上升沿)才 StartAnimationSequence(进场→循环)，为假 StopAnimation 并复位中性姿态；用 `_animActive` 做边沿检测避免每拍重置循环。
  - 新增 `EvalAnimWhen`（剥 `$...$` 后 `Ctx.Eval(...).AsBool()`，与触发器条件同约定）、`StartAnimationSequence`/`StopAnimation` 拆分、`TickAnimation()`（public）。
  - `AddCommonProps`/`ReadCommonProps` 持久化 `animWhen`。
  - `DirtyScheduler.Tick` 对每个原子(含容器子原子)调用 `a.TickAnimation()`，使带条件动画随数据变化起停。
- 动作参数下拉化（`Ui/PropertyEditorPanel.xaml.cs` + `LumenWindow.xaml.cs`）：
  - `BuildInteractionBlock` 重写：MediaControl→[播放/暂停/下一首/上一首/停止]、SwitchPreset→[上一个/下一个预设 + 各预设名]、SwitchPage→[下一页/上一页 + 第N页(按 PageCount)] 均用 ComboBox 下拉；RunApp/OpenURL/Command 保留文本框(+浏览)。新增 `BuildDropdown` 辅助(值=raw/显示=label)。
  - `LumenWindow` 暴露 `public int PageCount => _pages.Pages.Count;` 供切换页面下拉。
  - 本地化 zh-CN/en-GB 新增 `atom.label.animWhen`/`atom.hint.animWhen` 与 `prop.arg.*`(play/pause/next/prev/stop/presetNext/presetPrev/pageNext/pagePrev/pageN)，并更新 mediaControl/switchPage/switchPreset 提示为"从下拉选择"。
- 构建 0 错误 0 警告。
- 注意：`animWhen` 条件公式约定与触发器一致（可裸写 `mi(playing)="Playing"` 或 `$...$` 包裹、或 `gv:name`），不是 `$公式$` 自动解析为结果串。

## 触发器→流程 重命名 + 新动作类型（P5 行为系统大改）
- 需求：① 触发器(Trigger) 更名为 流程(Flow) 全局；② 点击动作下拉新增「触发流程(RunFlow)」可触发本原子某流程；③ 流程动作新增「读写变量(SetVar)」「延时(Delay)」「读取文件(ReadFile, txt/json)」。用户以「继续」接受推荐默认：流程按**序号**引用(非全局命名)、SetVar=`name|value`(值支持 `$公式$`/`$gv(x)$`，覆盖读写)、ReadFile=`路径|变量名[|json字段]`(写入全局变量)。
- 后端(已完成，构建 0 错误 0 警告)：
  - `AtomAction.cs` 枚举增 `RunFlow`/`SetVar`/`Delay`/`ReadFile`。
  - `Actions/AtomTrigger.cs`→`Actions/Flow.cs`：类 `AtomTrigger`→`Flow`、`TriggerFireMode`→`FlowFireMode`，序列化分隔符/格式不变(向后兼容)。
  - `ActionRunner.cs` 全改异步 `RunAllAsync`/`RunAsync`(返回 Task)：RunFlow 反查 `host.Flows[idx].Actions` 递归执行；SetVar 按 `|` 拆 `name|value`，值含 `$` 经 `win.Ctx.Eval` 解析(数值→Number 否则 Text)写 `win.Gv.Set`；Delay `Task.Delay(ms)`；ReadFile `File.ReadAllText`+可选 `JsonNavigate`(a.b.c / a[0].b) 写 `win.Gv`。补 `using System.Diagnostics;`(Process)。**流程循环引用防护**：RunFlow 用 `visiting` HashSet 调用栈，同一链上已执行的流程不再重入(防自调用死循环)，允许不同分支再次触发。
  - `Atoms/Atom.cs`：`Triggers`→`Flows`、`SupportsTrigger`→`SupportsFlow`(默认 true，tab key="flow")；`OnClickAction`→`ActionRunner.RunAsync`；`EvaluateTriggers`→`EvaluateFlows`；持久化 prop key 仍用 `"triggers"`(含 Flow.SerializeList/ParseList，旧单动作格式自动归首步)。补 `using System.Diagnostics;` 非必须。
  - `Engine/DirtyScheduler.cs`：`a.EvaluateFlows()` / `ch.EvaluateFlows()`。
  - `LumenWindow.xaml.cs`：已有 `internal GvStore Gv => _gv;` / `internal EvalContext Ctx => _ctx;` / `public int PageCount`（RunFlow/SetVar/ReadFile 依赖）。
- UI(`Ui/PropertyEditorPanel.xaml.cs`)：
  - 重命名：`BuildTriggerBlock`→`BuildFlowBlock`、`RebuildTriggerCards`→`RebuildFlowCards`、`BuildTriggerCard`→`BuildFlowCard`、`_triggerPanel`→`_flowPanel`；`KnownTabKeys`/`BuildTabs` 分支 `"trigger"`→`"flow"`；`UpdateTriggerConditionStatus`→`UpdateFlowConditionStatus`；文案 `prop.trigger.*`→`prop.flow.*`、`prop.tab.trigger`→`prop.tab.flow`。
  - 新增 `BuildCompositeArg(kind,curArg,setArg)` 共享组合参数控件：RunFlow=流程序号下拉(列 `_atom.Flows`，无流程时显示「暂无流程」)、SetVar=`名称|值` 双文本框、Delay=毫秒文本框、ReadFile=`路径文本框+浏览|写入变量|JSON字段(可选)` 纵向两行。
  - `ActionKindOptions()` 增 `RunFlow`；`BuildInteractionBlock` 与 `BuildActionEditor` 的 `RebuildArg`/`UpdateHint` 均接入 `BuildCompositeArg` 并补四种新类型的 hint。
  - `BuildActionEditor` 整体重构为 argHost + RebuildArg + UpdateHint（与交互块同构），去掉旧 argTb/browseBtn 内联逻辑。
- 语言包 zh-CN/en-GB：重命名 `prop.tab.trigger`→`prop.tab.flow`、`prop.trigger.*`→`prop.flow.*`(含 title/desc/add/when/condTooltip/presetCond/modeOnce/modeWhile/mode/exec/currentTrue)；新增 `prop.action.runFlow`、`prop.arg.{flowN,noFlow,varName,value,varValue,readVar,readJson}`、`prop.hint.{runFlow,setVar,delay,readFile}`、`dlg.textJson.filter`。
- 构建 0 错误 0 警告。`bin/Release` 内旧语言包仍含 `prop.trigger.*` 属旧构建产物，重新发布即覆盖。
- 已知设计权衡：RunFlow 按**序号**引用本原子流程，流程增删/重排会令序号漂移(无自动重映射)；用户已接受此简化。

## 编辑器重构 — KLWP 参考设计落档（2026-07-18 下午）
- 用户给 20 张 KLWP 编辑器截图，要求分析界面原型逻辑并落档。
- 已落档 `docs/编辑器重构设计.md`：全局三栏架构(列表/属性/工具+Overlay) + 属性编辑器 6 Tab(形状/油漆/图层/位置/动画/触摸) 字段级对照 + FormulaTextBox 三层结构(预览/输入/函数分类网格) + AddComponent 11卡片 + ProjectList Tab 体系 + UI 设计模式提炼 + Lumen 现状映射 + 重构优先级(P0 Tab分组+控件统一 / P1 公式编辑器增强+TreeWindow内嵌 / P2 添加组件面板+图层混合 / P3 序列等原子)。
- 关键结论：形状/位置/动画/流程 Lumen 已对齐或今日落地；**最大差距**=公式编辑器缺预览区+函数分类网格、图层缺混合/滤镜/模糊、控件风格(步进器vs文本框)待统一；**Lumen 差异化优势**=Shape 14种程序化质感(保留)。
- 8 个待拍板问题已列文档末尾（先做哪方向/TreeWindow内嵌否/控件方案/图层是否纳入/质感呈现/动画动作集扩展）。用户尚未拍板，下一步等用户选方向。

## MEMORY.md 清理 + 界面体系现状落档（2026-07-18 下午）
- 用户要求"整理落档目前相关设计的现状"。
- 先清理 `.workbuddy/memory/MEMORY.md`：原 13KB 含重复段（i18n 段两遍 + 结尾残留重复标题），精简去重到 ~3.2KB，保留选型/关键坑/结构/行为流程/i18n/Shape/容器/编辑器设计指针/构建。
- 派 Explore 子代理全面静态探查 `src/lumen` UI 现状（PropertyEditorPanel/Formula/TenantWindow/Atoms/Actions/各 Ui 窗），回报结构化清单。
- 落档 `docs/界面与编辑器体系现状.md`：10 章——体系总览(主窗+多浮窗分散式) / 主窗壳层 / 属性编辑器(已有6 Tab 但用 content/style/layout/animation/interaction/flow，非 KLWP 形状/油漆/图层/位置/动画/触摸；无步进器；无□公式方块视觉) / 公式引擎+函数目录(10分类) / TreeWindow(独立Window非内嵌) / 原子属性清单 / 流程行为系统 / 其他窗职责 / 现状vs设计缺口对照表 / 技术债。
- 关键现状发现：属性面板 Tab 骨架+三态值/公式/变量+函数选择器+字段预览已实装；缺=Tab重命名+图层页、步进器、□公式视觉、函数弹窗实时预览+卡片网格、TreeWindow内嵌、AddComponent卡片、图层混合/滤镜/模糊。
- 文档与设计文 `docs/编辑器重构设计.md` 配套（设计=目标，现状=真实代码）。

## 编辑器重构迁移决策 + 控件/公式弹窗设计稿（2026-07-18 傍晚）
- 用户已确认迁移方向：起点=控件统一 + 公式弹窗升级；TreeWindow=保持独立 Window 只改 UI；控件风格=保留 Lumen「值/公式/变量」三段切换 + 加 Stepper + 已绑公式红点。
- 已出 Ardot 设计稿：`Lumen编辑器迁移规范-控件与公式弹窗`（fileId `705185855733919`）。含双面板：左侧属性行控件规范（四段式/Stepper/九宫格/已绑红点），右侧公式弹窗升级（实时预览区+公式输入+工具栏+函数分类卡片网格）。
- 后续实现：按设计稿在 `PropertyEditorPanel` 引入新 Stepper 控件并统一字段行；升级函数选择器为卡片网格 + 实时预览区；公式/变量引擎侧不变。

## 设计稿修订（2026-07-18 傍晚，用户反馈）
用户修订控件/公式规范：
- 数值字段**不要步进按钮**，仅用输入框调整。
- 拖拽条保留，但**附加输入框**可键入精确值。
- 锚点九宫格体系通过（保留）。
- 公式展示**沿用旧的二级菜单（分类→函数）体系**，但整体重构（左侧分类列 + 右侧函数卡片 + 顶部公式输入/实时预览）。
已更新 Ardot 设计稿（同名文件 705185855733919）：Panel A 去掉 Stepper、宽度行改纯输入框、透明度行滑块+输入框；Panel B 由卡片网格弹窗改为二级菜单重构（分类列+函数列+顶部输入预览）。

## 设计稿落地代码（2026-07-18 晚，已构建 0 错 0 警）
按修订后设计稿把改动写入 `src/lumen/Ui/PropertyEditorPanel.xaml.cs` + lang：
- 新增 `MakeNumberBox(text,minWidth,onChanged)`：统一数值输入框深色样式（背景#1E1E1E/白字/灰边1/圆角/内边距）。
- `EditKind.Number` 字段改用 `MakeNumberBox`（原裸 TextBox）。
- `EditKind.Slider` 旁 num 文本框改用 `MakeNumberBox` 统一样式（保留拖拽条+输入框联动，滚轮/键入双向同步）。
- 字段行 `BuildFieldRow` 改为三列（标签/输入/状态点）；新增 `MakeModeDot`+`UpdateModeDot`：公式模式显示**红点**(prop.formula.bound)、其余**空心点**(prop.formula.unbound)；`MakeModeToggle` 点击时刷新该点。
- `OpenFunctionPicker` 整体重构（保留 分类→函数 二级菜单）：根网格改 4 行（标题/公式栏/主体/底部）；顶部新增**公式输入栏**（联动主 `st.FormulaTb.Text` 写回→触发 Preview/Status）+ **实时预览**块；左分类列选中高亮（`HighlightCat`，蓝底）；右函数卡片（Sig+Desc）点击插入光标处并关闭。
- lang(zh-CN/en-GB)：新增 `prop.formula.bound`/`prop.formula.unbound`/`prop.func.formula`/`prop.func.preview`。
- 锚点九宫格（GridModel/Coord）确认无需改动，原样保留。
- 构建 `dotnet build -c Release` 通过；未 git 提交。

## Flow/动作编辑逻辑解耦 + 公式选择器按已有公式重组（2026-07-18 晚，已构建 0 错 0 警）
用户指令：「流程解耦出来；公式选择器基于已有的公式决定重新组织内容」。

### 1. 流程解耦 → 新建 `Ui/ActionEditor.cs`
- 把 `PropertyEditorPanel` 内 ~560 行流程/动作编辑逻辑整段迁出到独立 `Lumen.Ui.ActionEditor` 类（实例持有 `_atom/_onPreview/_ctx`）。
- 承载：`ActionKindOptions()`(static)、`BuildDropdown()`(static)、`BuildCompositeArg()`(实例，RunFlow/SetVar/Delay/ReadFile 组合控件)、`BuildActionEditor()`(flow 步骤编辑)、`BuildFlowBlock()`/`RebuildFlowCards()`/`BuildFlowCard()`/`BuildFlowEditor()`/`UpdateFlowConditionStatus()`(流程子系统)。
- 改动点：`BuildCompositeArg` 与 `BuildActionEditor` 内 `Window.GetWindow(this)` 浏览框 owner 改为 `LumenWindow.Main`（ActionEditor 非 UI 元素，无 `this` 为 Visual）。
- `PropertyEditorPanel` 现状：删 `_flowPanel` 字段 + 三个共享 helper 方法定义 + 五个 Flow 方法定义；`BuildFlowBlock` 改为单行委托 `new ActionEditor(_atom,_onPreview,_ctx).BuildFlowBlock()`；`BuildInteractionBlock` 改用 `ActionEditor.ActionKindOptions()`/`ActionEditor.BuildDropdown()`/`ae.BuildCompositeArg()`（ae 实例）。interaction 段仍保留在 PropertyEditorPanel（点击动作编辑），仅动作/流程 UI 逻辑下沉 ActionEditor。
- 效果：流程/动作编辑成为可复用独立模块，PropertyEditorPanel 不再含业务逻辑；编译 0 错误 0 警告。

### 2. 公式选择器基于已有公式重组（`OpenFunctionPicker`）
- 打开时解析 `st.FormulaTb.Text` 已引用的函数名（手写扫描 `xxx(` 标识符，HashSet 忽略大小写）。
- 由此推出「已用分类」集合；左分类列按 `已用在前、其余在后` 排序；默认选中第一个已用分类（无则首类）。
- 已用分类按钮：前缀 `● ` + 加粗。
- 右函数列：当前分类下已用函数 `OrderBy(已用?0:1)` 置顶；已用函数卡片边框高亮绿(#34D399) + 顶部加 `● 已用` 文本标记（prop.func.used）。
- lang(zh-CN/en-GB) 新增 `prop.func.used`（已用/Used）。
- 顶部公式输入栏/实时预览保持不变（沿用上轮二级菜单重构）。

### 注意（架构事实，可入 MEMORY.md）
- Flow 编辑 UI 入口现统一在 `ActionEditor`；若未来 TreeWindow 或独立流程窗要复用，直接 `new ActionEditor(atom,onPreview,ctx).BuildFlowBlock()` 即可。
- 未 git 提交。

## 设计系统落地（代码）：全部弹窗统一 Theme 体系
- 建 `Ui/Theme.xaml`：15 色彩令牌 + 3 圆角令牌 + 8 类全局隐式样式（Button/TextBox/ComboBox/ListBox/TabControl/TabItem/TreeView/TreeViewItem/CheckBox/RadioButton）
- 建 `Ui/Theme.cs`：C# 画刷访问器（TryFindResource 读令牌）
- App.xaml 合并 Theme.xaml
- 7 个 XAML 弹窗去冗余本地样式、令牌化表面色（PropWindow/SettingsWindow/TreeWindow/ProfileWindow/GvManagerPanel/PageGridBgWindow/PropertyEditorPanel）
- FilePickerWindow：ToolWindow→None + 自定义标题栏 + 离群色收敛
- ColorPickerWindow/InputBox/ActionEditor/PropertyEditorPanel.xaml.cs：硬编码色替换为 Theme.*
- 写 `docs/Lumen 弹窗设计系统.md` 设计文档（色彩/圆角/控件/窗口结构/扩展指南）
- 构建 0 错误 0 警告
- Ardot 适配器离线，设计稿 705211484610740 未完成
